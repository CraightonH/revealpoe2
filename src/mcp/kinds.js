// src/mcp/kinds.js — the closed kind vocabulary. Own module (not in a backend
// file) because the pure tool layer needs it too and must not import a backend.
export const KINDS = ['gem', 'skill', 'base', 'unique', 'affix', 'tag', 'keyword',
  'class', 'passive', 'ascendancy', 'gear-slot', 'augment'];
