1. ~~Make a Build Planner (save groups of items, skills, supports, etc.)~~ ✅ done (Build Planner roadmap, Phases 1–8)
2. Make an Item Crafter
3. ~~Expand Theory Craft page to allow pinning items across searches~~ ✅ done (Build Planner roadmap Phase 6)
4. ~~All tooltips/cards get clickable icons for: 1. Add to Theory Craft~~ ✅ done (Build Planner roadmap Phase 6)
5. Ok I have a great idea for the wiki - import your character, change up your skills, some gear, etc. then ask the wiki to design gear items to make up for lacking stats: insufficient int now after skill swap? Missing 75% res? Have it design a piece to fill the gap and then have a trade link for that piece!
   - **5a. Character import** (prerequisite, researched 2026-09-08, parked): `docs/superpowers/specs/2026-09-08-character-import-research.md`. Decided: build a source-agnostic importer with pluggable adapters; PoB2 build-code paste is adapter #1 (zero backend, works today); GGG OAuth is adapter #2 via a same-origin Cloudflare Pages Function (not client-side — token endpoint has no CORS, web apps must be confidential clients, and our CSP is `connect-src 'self'`). **Blocked externally:** GGG's developer docs currently say "We are currently unable to process new applications", so no OAuth client id is obtainable; email `oauth@grindinggear.com` anyway. Brainstorm stopped at: how rare items land (best-effort mod mapping vs raw text vs both).
   - **5b. Gap-filling gear designer + trade link** — depends on 5a; not yet researched.
6. Rework the MCP integration. The `revealpoe2-mcp` worker + `revealpoe2-graph` D1 are currently orphaned: the weekly reseed in `refresh-data.yml` (job `seed-mcp`) is disabled as of 2026-09-07 because a full-graph reseed wrote ~253k rows per run — 2.5x the D1 free-tier cap of 100k rows/day — and the server wasn't worth the quota. Worth investigating: incremental seeding off the `sourceHash`/`manualHash` parity the schema already tracks, so only changed rows get written; or drop D1 entirely and serve the MCP from the static graph bundle the site already builds. Decide whether an MCP server earns its keep at all before rebuilding it.
