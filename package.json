{
  "name": "poe2wiki",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "build:graph": "node scripts/graph/cli.js",
    "build:index": "node scripts/build-index.js",
    "build:images": "env -u SSL_CERT_FILE -u NODE_EXTRA_CA_CERTS node scripts/fetch-images.js",
    "fetch:exchange-ids": "node scripts/fetch-exchange-ids.js",
    "build:og": "node scripts/build-og.js",
    "build:passives": "node scripts/build-passive-tree.js",
    "fetch:tree": "env -u SSL_CERT_FILE -u NODE_EXTRA_CA_CERTS node scripts/fetch-ggg-tree.js",
    "build:static": "npm run fetch:tree && npm run build:graph && npm run build:images && npm run build:index && npm run build:passives && npm run build:og && node scripts/prerender.js",
    "deploy": "npm run build:static && wrangler pages deploy dist --branch main",
    "deploy:preview": "npm run build:static && wrangler pages deploy dist --branch \"preview-$(git rev-parse --abbrev-ref HEAD)\"",
    "prestart": "npm run build:graph && npm run build:index && npm run build:passives",
    "predev": "npm run build:graph && npm run build:index && npm run build:passives",
    "pretest": "npm run build:graph && npm run build:passives"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "nunjucks": "^3.2.4",
    "satori": "^0.26.0",
    "sharp": "^0.35.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
