{
  "name": "poe2wiki",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "build:graph": "node scripts/graph/cli.js",
    "build:index": "node scripts/build-index.js",
    "build:images": "node scripts/fetch-images.js",
    "build:static": "npm run build:graph && npm run build:images && npm run build:index && node scripts/prerender.js",
    "deploy": "npm run build:static && wrangler pages deploy dist --branch main",
    "deploy:preview": "npm run build:static && wrangler pages deploy dist --branch \"preview-$(git rev-parse --abbrev-ref HEAD)\"",
    "prestart": "npm run build:graph && npm run build:index",
    "predev": "npm run build:graph && npm run build:index",
    "pretest": "npm run build:graph"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
